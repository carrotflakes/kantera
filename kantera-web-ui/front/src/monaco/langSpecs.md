# symbolDescriptions
## composite
composite

```scheme
(composite
  (vec render_1 'normal)
  (vec render_2 'normal))
```

## plain
plain

```scheme
(plain (rgb 1.0 0.0 0.0))
```

## path
path

```scheme
(path initial_value
  (vec dtime second_value interpolation)
  ...)
```
